import React from 'react'

const Settings: React.FC = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h2>設定（プレースホルダ）</h2>
      <p>ここにアプリケーション設定を表示します。</p>
    </div>
  )
}

export default Settings
